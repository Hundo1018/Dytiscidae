# Core (CPU-only, portable). Everything below installs without a GPU.
numpy>=1.26
scipy>=1.11
mujoco>=3.1

# Visualisation / observability
matplotlib>=3.8
imageio>=2.34
imageio-ffmpeg>=0.4.9

# Optional accelerators -- the system runs without them.
#   torch          : needed for --shared-policy (learning/ppo.py). CPU build
#                    is the right one: the network is ~1200 weights, where a
#                    CUDA launch costs more than the arithmetic.
#                    CPU wheel is fine; CUDA wheel is used automatically if present.
#   ray            : only needed for multi-machine parallel evaluation.
torch>=2.2  # --index-url https://download.pytorch.org/whl/cpu
# ray>=2.9
